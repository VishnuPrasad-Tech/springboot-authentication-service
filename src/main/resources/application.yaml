spring:
  application:
    name: springboot-authentication-service
  datasource:
    url: jdbc:postgresql://localhost:5432/springboot_auth_db
    username: postgres
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true
    properties:
      hibernate:
        format_sql: true

